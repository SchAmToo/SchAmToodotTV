<p>
	<% if current_user.nil? %>
		<%= link_to "Log In", login_path %>
		<%= link_to "Sign Up", users_signup_path %>
	<% else %>
		<%= link_to User.find(current_user).login, user_path(User.find(current_user.id).login) %>
		<%= link_to 'Log Out', logout_path %>
	<% end %>
</p>