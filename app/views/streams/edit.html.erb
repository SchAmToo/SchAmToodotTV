<%= render "sessions/session" %>

<% if current_user?(User.find_by_login("Schamtoo")) %>

<h1>Editing Streams</h1>
<% for stream in Stream.all %> 
	<%= render partial: 'form', locals: {path: {controller: "streams", action: (edit)}}%>
<% end %>

<%= link_to 'Back', posts_path %>
<% else %>
<h1>403 Forbidden</h1>
<%end%>